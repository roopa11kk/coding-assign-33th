Driver code starts
// initial template for c
#include<stdio.h>

//}Driver code ends
//user function template for c

int josephus(int n, int k)
{
  // your code here
  if(n==1)
  return 1;
  else
  return(joephus(n-1,k)+k-1)%n+1;
  }//{
  //{Driver code starts
  
  int main(){
  int t;
  scanf("%d",&t);//test cases
  while(t--)
  {
  int n,k;
  scanf("%d%d",&n,&k);//taking input n and k
  
  //calling josephus() function
  printf("%d\n",josephus(n,k));
  }
  return 0;
  }
  //} Driver code ends
